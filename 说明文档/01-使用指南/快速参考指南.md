# 课程管理系统 - 快速参考指南

## 🎯 核心功能一览

### 1. 课程管理 (Course Management)
| 功能 | URL | 方法 | 说明 |
|------|-----|------|------|
| 查看课程列表 | `/api/courses/courses` | GET | 支持搜索、筛选 |
| 创建课程 | `/api/courses/courses/` | POST | 必填:名称、类型、教师、专业 |
| 更新课程 | `/api/courses/courses/{id}/` | PATCH | 部分更新 |
| 删除课程 | `/api/courses/courses/{id}/` | DELETE | 删除课程 |

### 2. 排课管理 (Schedule Management)
| 功能 | URL | 方法 | 说明 |
|------|-----|------|------|
| 查看课程表 | `/api/courses/schedules` | GET | 按班级/教师/教室查看 |
| 创建排课 | `/api/courses/schedules/` | POST | 自动冲突检测 |
| 移动排课 | `/api/courses/schedules/{id}/` | PATCH | 更改时间段 |
| 删除排课 | `/api/courses/schedules/{id}/` | DELETE | 删除排课记录 |

### 3. 智能功能 (Smart Features)
| 功能 | URL | 方法 | 说明 |
|------|-----|------|------|
| 智能排课 | `/api/courses/auto-schedule/` | POST | 自动安排课程 |
| 冲突检测 | `/api/courses/schedules/conflicts/` | GET | 检测时间冲突 |
| 生成时间段 | `/api/courses/timeslots/generate` | POST | 生成标准课表 |

---

## 📊 数据模型速查

### Course (课程)
```python
{
    "id": 1,
    "subject_id": "SUB001",        # 自动生成
    "name": "高等数学",
    "course_type": "required",     # required/elective/practical
    "edu_level": "undergraduate",  # 教育层级
    "teacher": 5,                  # 教师ID
    "department": 10               # 专业ID
}
```

### CourseSchedule (排课)
```python
{
    "id": 100,
    "school_class": 1,    # 班级ID
    "course": 1,          # 课程ID
    "teacher": 5,         # 教师ID
    "classroom": 15,      # 教室ID (可选)
    "timeslot": 3,        # 时间段ID
    "week_number": 1      # 周次 (1-20)
}
```

### TimeSlot (时间段)
```python
{
    "id": 1,
    "weekday": 1,              # 1-7 (周一到周日)
    "start_time": "08:30:00",
    "end_time": "09:15:00",
    "index": 1                 # 节次 (1-8)
}
```

---

## 🔧 常用API调用示例

### JavaScript 前端调用

#### 1. 搜索课程
```javascript
const params = new URLSearchParams({
    q: '高等数学',
    department: 1,
    college: 2
});

const courses = await api(`/api/courses/courses?${params}`);
```

#### 2. 创建课程
```javascript
const newCourse = await api('/api/courses/courses/', 'POST', {
    name: '数据结构',
    course_type: 'required',
    teacher: 5,
    department: 10,
    edu_level: 'undergraduate'
});
```

#### 3. 查看班级课程表
```javascript
const schedule = await api('/api/courses/schedules?school_class=1&week_number=1');
```

#### 4. 手动排课
```javascript
const newSchedule = await api('/api/courses/schedules/', 'POST', {
    school_class: 1,
    course: 1,
    teacher: 5,
    classroom: 15,
    timeslot: 3,
    week_number: 1
});
```

#### 5. 智能排课
```javascript
const result = await api('/api/courses/auto-schedule/', 'POST', {
    school_class: 1,
    courses: [1, 2, 3, 4],
    start_week: 1,
    end_week: 18,
    week_mode: 'all'  // all/odd/even
});

console.log(`成功排课 ${result.created_count} 节`);
```

#### 6. 冲突检测
```javascript
const conflicts = await api('/api/courses/schedules/conflicts/?school_class=1&week_number=1');

if (conflicts.count > 0) {
    console.log(`发现 ${conflicts.count} 处冲突`);
    conflicts.items.forEach(c => {
        console.log(`时间段 ${c.timeslot}, 冲突类型:`, {
            teacher: c.teacher_conflicts,
            class: c.class_conflicts,
            room: c.room_conflicts
        });
    });
}
```

---

## 🎨 前端组件速查

### 课程卡片 (拖拽元素)
```html
<div class="drag-card" draggable="true" data-course="1">
    <div>高等数学</div>
</div>
```

### 课程表格子
```html
<div class="tt-cell" data-weekday="1" data-index="2">
    <!-- 拖放目标 -->
</div>
```

### 课程块 (课程表中)
```html
<div class="tt-block" data-schedule-id="100">
    <div class="tt-course-name">高等数学</div>
    <div class="tt-info">教师: 张三</div>
    <div class="tt-info">教室: A101</div>
    <span class="tt-del" onclick="deleteSchedule(100)">×</span>
</div>
```

### 冲突标记
```css
.tt-block.conflict {
    border: 2px solid #ef4444;
    animation: pulse 2s infinite;
}
```

---

## ⚡ 关键函数速查

### 数据加载
```javascript
loadCourses()       // 加载课程列表
loadSchedule()      // 加载课程表
loadTeachers()      // 加载教师列表
loadClasses()       // 加载班级列表
loadRooms()         // 加载教室列表
initTimetable()     // 初始化课程表网格
```

### 课程操作
```javascript
addCourse()         // 新增课程
updateCourse()      // 更新课程
deleteSelectedCourses()  // 删除选中课程
renderPalette()     // 渲染拖拽面板
```

### 排课操作
```javascript
createSchedule(courseId, weekday, index)  // 创建排课
moveSchedule(scheduleId, weekday, index)  // 移动排课
deleteSchedule(scheduleId)                // 删除排课
autoSchedule()                            // 智能排课
checkConflicts()                          // 冲突检测
```

### 视图控制
```javascript
changeViewMode()    // 切换视图模式 (班级/教师/教室)
prevWeek()          // 上一周
nextWeek()          // 下一周
renderTimetableGrid()  // 渲染课程表网格
renderSchedule(schedules)  // 渲染排课数据
```

### 拖拽控制
```javascript
handleDragOver(ev)  // 拖拽悬停
handleDrop(ev)      // 放下处理
handleDragLeave(ev) // 离开处理
```

---

## 🚨 常见错误及解决

### 1. "教师在该时间段已有课程安排"
**原因**: 同一教师在同一时间段有多节课  
**解决**: 更换时间段或更换教师

### 2. "班级在该时间段已有课程安排"
**原因**: 同一班级在同一时间段有多节课  
**解决**: 更换时间段

### 3. "教室在该时间段已被占用"
**原因**: 同一教室在同一时间段被多次使用  
**解决**: 更换教室或更换时间段

### 4. "教室容量不足(30),班级人数为45"
**原因**: 教室容量小于班级人数  
**解决**: 选择更大的教室

### 5. "该名称的课程已存在"
**原因**: 同一教育层级下课程名称重复  
**解决**: 修改课程名称或选择已有课程

---

## 🔐 权限控制速查

| 角色 | 课程管理 | 查看课程表 | 排课 | 智能排课 | 冲突检测 |
|------|---------|-----------|------|---------|---------|
| **超级管理员** | ✅ 全部 | ✅ 全部 | ✅ 全部 | ✅ | ✅ |
| **校长/副校长** | ✅ 全部 | ✅ 全部 | ✅ 全部 | ✅ | ✅ |
| **教务长** | ✅ 全部 | ✅ 全部 | ✅ 全部 | ✅ | ✅ |
| **教师** | ❌ | ✅ 自己的 | ✅ 自己的 | ❌ | ✅ |
| **学生** | ❌ | ✅ 自己的 | ❌ | ❌ | ❌ |

---

## 📝 数据库查询优化

### 使用 select_related 优化
```python
# ❌ 不好 (N+1 查询问题)
courses = Course.objects.all()
for course in courses:
    print(course.teacher.user_profile.user.first_name)  # 每次都查询数据库

# ✅ 好 (一次查询)
courses = Course.objects.select_related(
    'teacher__user_profile__user',
    'department__college'
).all()
```

### 课程表查询优化
```python
schedules = CourseSchedule.objects.select_related(
    'school_class',
    'course',
    'teacher__user_profile__user',
    'classroom',
    'timeslot'
).filter(school_class_id=1, week_number=1)
```

---

## 🎯 智能排课算法核心

### 评分规则
```python
score = 0

# 规则1: 必修课优先上午
if course.type == 'required' and timeslot.index <= 4:
    score += 5

# 规则2: 优先早节次
score += (10 - timeslot.index)

# 最终选择得分最高的时间段
```

### 评分表
| 课程类型 | 时间段 | 得分 | 优先级 |
|---------|--------|------|--------|
| 必修 | 周一第1节 | 14 | ⭐⭐⭐⭐⭐ |
| 必修 | 周一第2节 | 13 | ⭐⭐⭐⭐⭐ |
| 必修 | 周一第4节 | 11 | ⭐⭐⭐⭐ |
| 选修 | 周一第1节 | 9 | ⭐⭐⭐⭐ |
| 选修 | 周一第6节 | 4 | ⭐⭐ |
| 选修 | 周一第8节 | 2 | ⭐ |

---

## 🔍 调试技巧

### 1. 查看API请求
```javascript
// 在浏览器控制台
window.api = async (url, method = 'GET', data = null) => {
    console.log(`[API] ${method} ${url}`, data);
    const response = await fetch(url, {
        method,
        headers: { 'Content-Type': 'application/json' },
        body: data ? JSON.stringify(data) : null
    });
    const result = await response.json();
    console.log(`[API] Response:`, result);
    return result;
};
```

### 2. 查看拖拽数据
```javascript
document.addEventListener('dragstart', (e) => {
    console.log('[Drag] Start:', e.dataTransfer.getData('text/plain'));
});

document.addEventListener('drop', (e) => {
    console.log('[Drag] Drop:', e.dataTransfer.getData('text/plain'));
});
```

### 3. 查看课程表状态
```javascript
// 在浏览器控制台
console.log('当前周次:', currentWeek);
console.log('视图模式:', currentViewMode);
console.log('选中对象:', currentTargetId);
console.log('课程数据:', tt.courses);
console.log('时间段数据:', tt.slots);
console.log('排课数据:', scheduleData);
```

---

## 📱 快捷键 (未来扩展)

| 快捷键 | 功能 |
|--------|------|
| `Ctrl + F` | 搜索课程 |
| `Ctrl + N` | 新增课程 |
| `Ctrl + S` | 保存更改 |
| `←` / `→` | 切换周次 |
| `Delete` | 删除选中排课 |
| `Ctrl + Z` | 撤销操作 |

---

## 🔗 相关文档

1. **[COURSE_MANAGEMENT_DESIGN.md](./COURSE_MANAGEMENT_DESIGN.md)** - 完整设计文档
   - 系统架构
   - 数据库设计
   - 业务逻辑
   - API接口详解

2. **[COURSE_WORKFLOW_DIAGRAM.md](./COURSE_WORKFLOW_DIAGRAM.md)** - 工作流程图
   - 架构图
   - 数据流向图
   - 时序图
   - 算法流程图

3. **[courses/models.py](./courses/models.py)** - 数据模型定义

4. **[courses/views.py](./courses/views.py)** - 后端视图逻辑

5. **[courses/serializers.py](./courses/serializers.py)** - 数据序列化与校验

6. **[static/js/courses/courses.js](./static/js/courses/courses.js)** - 前端逻辑

7. **[templates/courses.html](./templates/courses.html)** - 页面模板

---

## 🎓 跨学院授课支持

### 系统完全支持跨学院/专业授课！

**使用方法**:
1. 在创建课程时，勾选"显示所有教师（跨学院/专业）"
2. 即可选择全校任何教师授课
3. 排课时无学院/专业限制

**典型场景**:
```
数学学院张老师 → 计算机学院学生（高等数学）
外语学院李老师 → 全校各专业学生（大学英语）
体育学院王老师 → 全校学生（体育课）
```

**前端操作**:
```javascript
// 显示所有教师
document.getElementById('showAllTeachers').checked = true;
toggleTeacherFilter();

// 创建跨学院课程
await api('/api/courses/courses/', 'POST', {
    name: '高等数学',
    teacher: 5,              // 数学学院教师
    department: 10,          // 归属计算机专业
    course_type: 'required',
    edu_level: 'undergraduate'
});
```

## 💡 最佳实践

### 1. 排课前准备
1. 先创建所有课程
2. 生成标准时间段 (`POST /api/courses/timeslots/generate`)
3. 确认教师、班级、教室信息完整

### 2. 排课流程
1. 选择视图模式(按班级)
2. 选择目标班级
3. 选择要排的课程
4. 先手动排重要课程(主课)
5. 使用智能排课处理剩余课程
6. 运行冲突检测
7. 手动调整冲突课程

### 3. 批量操作
```javascript
// 为多个班级排同一门课程
const classes = [1, 2, 3, 4, 5];
for (const classId of classes) {
    await api('/api/courses/auto-schedule/', 'POST', {
        school_class: classId,
        courses: [1],  // 高等数学
        start_week: 1,
        end_week: 18,
        week_mode: 'all'
    });
}
```

### 4. 性能优化
- 使用缓存 (`cache.teachers`, `cache.classes`)
- 防抖搜索 (300ms延迟)
- 批量操作使用事务
- 定期清理过期排课数据

### 5. 跨学院授课管理
**公共基础课建议**:
- 为公共课创建统一的"公共课程"分类
- 课程命名包含受众: "高等数学(理工类)"
- 大规模公共课可使用教学班模式

**教师选择技巧**:
- 默认: 按学院/专业筛选（快速定位）
- 跨学院: 勾选"显示所有教师"（灵活选择）

---

## 🎓 学习路径

### 新手入门 (1-2天)
1. 阅读 `QUICK_REFERENCE.md` (本文档)
2. 浏览 `courses.html` 了解页面结构
3. 尝试手动创建课程和排课
4. 理解基础API调用

### 进阶开发 (3-5天)
1. 阅读 `COURSE_MANAGEMENT_DESIGN.md`
2. 研究 `courses.js` 前端逻辑
3. 学习拖拽API实现
4. 理解冲突检测机制

### 高级定制 (1-2周)
1. 阅读 `COURSE_WORKFLOW_DIAGRAM.md`
2. 研究智能排课算法
3. 修改评分规则
4. 扩展新功能

---

## 📞 获取帮助

### 常见问题排查
1. **API调用失败**
   - 检查用户权限
   - 查看浏览器控制台错误
   - 验证请求数据格式

2. **拖拽不工作**
   - 确认元素有 `draggable="true"`
   - 检查事件监听器是否绑定
   - 查看 `dataTransfer` 数据是否正确

3. **课程表不显示**
   - 确认已选择视图模式和目标
   - 检查时间段是否生成
   - 验证排课数据是否存在

---

**版本**: 1.0  
**更新**: 2025-12-09  
**维护**: EduCloud Team

