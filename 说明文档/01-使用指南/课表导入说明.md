# 📚 课表批量导入工具 - 使用指南

## 🎯 功能说明

本工具用于批量导入班级课表，自动创建：
- ✅ 教师账号
- ✅ 课程信息
- ✅ 教室信息
- ✅ 排课记录

---

## 📦 文件清单

| 文件名 | 说明 |
|--------|------|
| `import_class_2023502701_schedule.py` | 主导入脚本（Python） |
| `import_schedule_2023502701.bat` | Windows快速启动脚本 |
| `check_before_import.py` | 导入前检查脚本 |
| `SCHEDULE_IMPORT_2023502701.md` | 详细导入说明文档 |
| `README_IMPORT_SCHEDULE.md` | 本文件（快速上手指南） |

---

## 🚀 快速开始（3步搞定）

### 第1步：检查系统状态

**Windows用户**：
```cmd
python check_before_import.py
```

**预期输出**：
```
【检查1】班级是否存在...
  ✓ 班级存在: 23移动互联网应用技术G5-1班
【检查2】时间段是否初始化...
  ✓ 时间段已就绪 (共 40 个)
【检查3】管理员用户...
  ✓ 管理员用户存在: admin
【检查4】检查已有数据...
  ✓ 没有教师冲突

✓ 所有检查通过！可以开始导入课表
```

### 第2步：执行导入

**Windows用户（推荐）**：
```
双击运行: import_schedule_2023502701.bat
```

**命令行用户**：
```cmd
python import_class_2023502701_schedule.py
```

### 第3步：验证结果

访问系统查看：
```
http://localhost:8000/courses
```

选择"按班级"视图，选择"2023502701"班，查看课表。

---

## 📋 导入内容一览

### 👨‍🏫 教师（6位）
- 李雪莲 (T2024001)
- 刘欢 (T2024002)
- 王明超 (T2024003)
- 宋小莹 (T2024004)
- 李新宇 (T2024005)
- 梁语馨 (T2024006)

**默认密码**: `123456`

### 📚 课程（6门）
- ArkTS基础应用开发 (李雪莲)
- 历史 (刘欢)
- 移动web开发 (王明超)
- 商务英语 (宋小莹)
- 数据库技术与应用 (李新宇)
- Python项目开发 (梁语馨)

### 🏫 教室（6间）
- 507 (5号楼)
- 215 (2号楼)
- 505 (5号楼)
- 404 (4号楼)
- 西山II1411
- 西山II1101

### 📅 排课（11节/周）
详见下方课表。

---

## 📅 课表详情

```
┌─────────┬──────────────────────┬──────────────────────┬──────────────────────┬──────────────────────┬──────────────────────┐
│  时间    │      星期一          │      星期二          │      星期三          │      星期四          │      星期五          │
├─────────┼──────────────────────┼──────────────────────┼──────────────────────┼──────────────────────┼──────────────────────┤
│ 第1节   │ ArkTS基础应用开发    │ 历史                 │                      │ ArkTS基础应用开发    │                      │
│ 08:30   │ 李雪莲 / 507         │ 刘欢 / 215           │                      │ 李雪莲 / 507         │                      │
├─────────┼──────────────────────┼──────────────────────┼──────────────────────┼──────────────────────┼──────────────────────┤
│ 第2节   │                      │                      │ 移动web开发          │                      │ 移动web开发          │
│ 09:25   │                      │                      │ 王明超 / 505         │                      │ 王明超 / 505         │
├─────────┼──────────────────────┼──────────────────────┼──────────────────────┼──────────────────────┼──────────────────────┤
│ 第3节   │ 商务英语             │ 商务英语             │                      │                      │                      │
│ 10:35   │ 宋小莹 / 404         │ 宋小莹 / 404         │                      │                      │                      │
├─────────┼──────────────────────┼──────────────────────┼──────────────────────┼──────────────────────┼──────────────────────┤
│ 第5节   │ 数据库技术与应用     │ Python项目开发       │ Python项目开发       │ 数据库技术与应用     │                      │
│ 14:00   │ 李新宇 / 西山II1411  │ 梁语馨 / 西山II1101  │ 梁语馨 / 西山II1101  │ 李新宇 / 西山II1411  │                      │
└─────────┴──────────────────────┴──────────────────────┴──────────────────────┴──────────────────────┴──────────────────────┘
```

---

## ⚠️ 常见问题

### ❓ 问题1：班级不存在

**错误信息**：
```
✗ 班级不存在，需要先创建班级！
```

**解决方法**：

**方法A - Web界面**（推荐）：
1. 登录系统管理员账号
2. 访问组织架构管理
3. 创建班级：
   - 班级ID: `2023502701`
   - 班级名称: `23移动互联网应用技术G5-1班`
   - 年级: `2023`
   - 选择专业

**方法B - Django Shell**：
```bash
python manage.py shell
```
```python
from organization.models import Class, Major

# 获取专业（根据实际情况选择）
dept = Major.objects.filter(name__contains='移动互联网').first()
# 如果没有，选择第一个专业
if not dept:
    dept = Major.objects.first()

# 创建班级
Class.objects.create(
    class_id='2023502701',
    name='23移动互联网应用技术G5-1班',
    department=dept,
    grade=2023,
    class_type='administrative'
)

print("班级创建成功！")
```

### ❓ 问题2：时间段未初始化

**错误信息**：
```
✗ 时间段未初始化！
```

**解决方法**：

**方法A - Web界面**（推荐）：
1. 访问 `http://localhost:8000/courses`
2. （如有生成时间段按钮）点击生成

**方法B - API调用**：
```bash
curl -X POST http://localhost:8000/api/courses/timeslots/generate
```

**方法C - Django Shell**：
```bash
python manage.py shell
```
```python
from courses.models import TimeSlot
import datetime

def create_timeslots():
    # 上午课程时间
    morning = [
        (8, 30, 9, 15),   # 第1节
        (9, 25, 10, 10),  # 第2节
        (10, 35, 11, 20), # 第3节
        (11, 30, 12, 15), # 第4节
    ]
    
    # 下午课程时间
    afternoon = [
        (14, 0, 14, 45),  # 第5节
        (14, 55, 15, 40), # 第6节
        (16, 5, 16, 50),  # 第7节
        (17, 0, 17, 45),  # 第8节
    ]
    
    for weekday in range(1, 6):  # 周一到周五
        index = 1
        for start_h, start_m, end_h, end_m in morning + afternoon:
            TimeSlot.objects.get_or_create(
                weekday=weekday,
                index=index,
                defaults={
                    'start_time': f'{start_h:02d}:{start_m:02d}:00',
                    'end_time': f'{end_h:02d}:{end_m:02d}:00'
                }
            )
            index += 1
    
    print(f"时间段创建成功！共 {TimeSlot.objects.count()} 个")

create_timeslots()
```

### ❓ 问题3：教师工号冲突

**错误信息**：
```
IntegrityError: UNIQUE constraint failed: accounts_teacherprofile.teacher_id
```

**解决方法**：

编辑 `import_class_2023502701_schedule.py`，修改教师工号：

```python
teachers = {
    'lixuelian': get_or_create_teacher('T2024101', '李雪莲', department),  # 修改工号
    'liuhuan': get_or_create_teacher('T2024102', '刘欢', department),
    # ... 其他教师
}
```

### ❓ 问题4：重复导入

**症状**：再次运行脚本时看到"已存在"提示

**说明**：这是正常的！脚本会自动跳过已存在的数据，只创建缺失的部分。

**验证方法**：
```bash
python manage.py shell
```
```python
from courses.models import CourseSchedule
from organization.models import Class

cls = Class.objects.get(class_id='2023502701')
count = CourseSchedule.objects.filter(school_class=cls, week_number=1).count()
print(f"已有排课: {count} 节")  # 应该是11节
```

---

## 🔧 高级功能

### 为多个周次创建排课

默认只创建第1周的排课。如需创建第1-18周：

编辑 `import_class_2023502701_schedule.py`，找到"创建排课"部分，修改为：

```python
# 【步骤6】创建排课...
print("【步骤6】创建排课...")

success_count = 0
for week in range(1, 19):  # 1-18周
    print(f"\n  处理第 {week} 周...")
    for item in schedule_data:
        timeslot = get_timeslot(item['day'], item['period'])
        if not timeslot:
            continue
        
        schedule = create_schedule(
            school_class=school_class,
            course=courses[item['course']],
            teacher=teachers[item['teacher']],
            classroom=classrooms[item['room']],
            timeslot=timeslot,
            week_number=week,  # 使用循环的周次
            created_by=admin_user
        )
        if schedule:
            success_count += 1

print(f"\n总计创建 {success_count} 条排课记录")
```

### 清除已导入数据

如需重新导入，先清除数据：

```bash
python manage.py shell
```
```python
from courses.models import CourseSchedule, Course
from accounts.models import TeacherProfile
from organization.models import Class

# 删除排课
cls = Class.objects.get(class_id='2023502701')
schedules = CourseSchedule.objects.filter(school_class=cls)
print(f"删除排课: {schedules.count()} 条")
schedules.delete()

# 删除课程（可选）
courses = Course.objects.filter(department=cls.department)
print(f"删除课程: {courses.count()} 门")
courses.delete()

# 删除教师（可选，谨慎操作）
teachers = TeacherProfile.objects.filter(teacher_id__startswith='T2024')
print(f"删除教师: {teachers.count()} 位")
# teachers.delete()  # 取消注释以执行删除

print("清理完成！")
```

---

## 📊 验证导入结果

### 方法1：Web界面

1. 访问课程管理：`http://localhost:8000/courses`
2. 选择视图：`按班级`
3. 选择班级：`2023502701`
4. 选择周次：`第1周`
5. 查看课表是否正确显示

### 方法2：Django Shell

```bash
python manage.py shell
```
```python
from courses.models import CourseSchedule
from organization.models import Class

cls = Class.objects.get(class_id='2023502701')
schedules = CourseSchedule.objects.filter(
    school_class=cls,
    week_number=1
).select_related('course', 'teacher', 'classroom', 'timeslot')

print(f"班级: {cls.name}")
print(f"排课数量: {schedules.count()} 节\n")

for s in schedules.order_by('timeslot__weekday', 'timeslot__index'):
    weekdays = ['', '周一', '周二', '周三', '周四', '周五', '周六', '周日']
    print(f"{weekdays[s.timeslot.weekday]}第{s.timeslot.index}节: "
          f"{s.course.name} | "
          f"{s.teacher.user_profile.user.first_name} | "
          f"{s.classroom.building}-{s.classroom.room_number}")
```

预期输出：
```
班级: 23移动互联网应用技术G5-1班
排课数量: 11 节

周一第1节: ArkTS基础应用开发 | 李雪莲 | 5号楼-507
周一第3节: 商务英语 | 宋小莹 | 4号楼-404
周一第5节: 数据库技术与应用 | 李新宇 | 西山II-1411
周二第1节: 历史 | 刘欢 | 2号楼-215
周二第3节: 商务英语 | 宋小莹 | 4号楼-404
周二第5节: Python项目开发 | 梁语馨 | 西山II-1101
周三第2节: 移动web开发 | 王明超 | 5号楼-505
周三第5节: Python项目开发 | 梁语馨 | 西山II-1101
周四第1节: ArkTS基础应用开发 | 李雪莲 | 5号楼-507
周四第5节: 数据库技术与应用 | 李新宇 | 西山II-1411
周五第2节: 移动web开发 | 王明超 | 5号楼-505
```

---

## 📞 获取帮助

### 文档资源
- 📘 [详细导入说明](./SCHEDULE_IMPORT_2023502701.md)
- 📗 [课程管理设计文档](./COURSE_MANAGEMENT_DESIGN.md)
- 📙 [快速参考指南](./QUICK_REFERENCE.md)

### 常见操作
```bash
# 检查系统状态
python check_before_import.py

# 执行导入
python import_class_2023502701_schedule.py

# Django Shell
python manage.py shell

# 启动服务器
python manage.py runserver
```

---

## ✅ 完整流程示例

```bash
# 1. 检查系统
python check_before_import.py

# 2. 如果班级不存在，创建班级（Web界面或Django Shell）

# 3. 如果时间段未初始化，生成时间段（Web界面或API）

# 4. 执行导入
python import_class_2023502701_schedule.py

# 5. 验证结果
python manage.py shell
>>> from courses.models import CourseSchedule
>>> from organization.models import Class
>>> cls = Class.objects.get(class_id='2023502701')
>>> print(f"排课数量: {CourseSchedule.objects.filter(school_class=cls, week_number=1).count()}")
排课数量: 11

# 6. 访问Web界面查看
http://localhost:8000/courses
```

---

**版本**: 1.0  
**更新日期**: 2025-12-09  
**适用班级**: 2023502701 (23移动互联网应用技术G5-1班)

**祝使用愉快！** 🎉

