# 课程管理系统设计文档

## 📋 目录
1. [系统架构](#系统架构)
2. [数据库设计](#数据库设计)
3. [前端设计](#前端设计)
4. [业务逻辑](#业务逻辑)
5. [API接口](#api接口)
6. [功能流程](#功能流程)

---

## 系统架构

### 技术栈
- **后端**: Django + Django REST Framework
- **前端**: Vanilla JavaScript + Bootstrap 5
- **数据库**: SQLite/PostgreSQL (通过Django ORM)
- **UI风格**: 现代扁平化设计 (Modern Flat Design)

### 模块划分
```
课程管理系统
├── 课程库管理 (Course Library)
│   ├── 课程CRUD
│   ├── 课程搜索/筛选
│   └── 课程选择/拖拽
├── 课程表管理 (Timetable)
│   ├── 多维度查看 (班级/教师/教室)
│   ├── 拖拽排课
│   └── 周次切换
└── 智能排课 (Auto-scheduling)
    ├── 自动排课引擎
    └── 冲突检测
```

---

## 数据库设计

### 1. Course (课程表)
```python
class Course(models.Model):
    subject_id = CharField(max_length=64, unique=True)      # 课程编号
    name = CharField(max_length=128)                        # 课程名称
    course_type = CharField(choices=COURSE_TYPES)           # 必修/选修/实践
    edu_level = CharField(choices=EDU_LEVELS)               # 教育层级
    teacher = ForeignKey(TeacherProfile)                    # 授课教师
    department = ForeignKey(Major)                          # 所属专业
```

**字段说明**:
- `subject_id`: 自动生成唯一标识 (如: SUB001, SUBA3F2C...)
- `course_type`: required(必修), elective(选修), practical(实践)
- `edu_level`: high_school(高中), undergraduate(本科)等
- **唯一约束**: (name, edu_level) - 同一教育层级下课程名称唯一

### 2. TimeSlot (时间段表)
```python
class TimeSlot(models.Model):
    weekday = IntegerField()        # 星期几 (1-7)
    start_time = TimeField()        # 开始时间
    end_time = TimeField()          # 结束时间
    index = IntegerField()          # 节次索引 (1-8)
```

**标准时间配置**:
- 上午: 4节课 (08:30-12:00)
- 下午: 4节课 (14:00-17:30)
- 每节课45分钟,课间10分钟

### 3. CourseSchedule (课程表)
```python
class CourseSchedule(models.Model):
    school_class = ForeignKey(SchoolClass)    # 上课班级
    course = ForeignKey(Course)               # 课程
    teacher = ForeignKey(TeacherProfile)      # 授课教师
    classroom = ForeignKey(Classroom)         # 教室
    timeslot = ForeignKey(TimeSlot)          # 时间段
    week_number = IntegerField()              # 周次 (1-20)
    created_by = ForeignKey(User)             # 创建人
```

**冲突检测约束**:
1. 同一时间段+周次,同一班级只能有一门课
2. 同一时间段+周次,同一教师只能上一节课
3. 同一时间段+周次,同一教室只能被使用一次
4. 教室容量必须≥班级人数

---

## 前端设计

### 页面布局 (Left-Right Split)

```
┌─────────────────────────────────────────────────────────────┐
│  课程管理                                      [用户] [登出]  │
├────────────────┬────────────────────────────────────────────┤
│                │  课程表                                     │
│  📚 课程管理    │  [按班级▼] [2024级计算机1班▼]              │
│                │  [◀] 第1周 [▶]                             │
│  [搜索框]      │                                            │
│  [学院▼][专业▼]│  ┌─────┬────┬────┬────┬────┬────┬────┐   │
│                │  │时间 │周一│周二│周三│周四│周五│周六│周日│   │
│  新增课程      │  ├─────┼────┼────┼────┼────┼────┼────┤   │
│  [课程名]      │  │08:30│    │    │    │    │    │    │   │
│  [必修▼]       │  │09:25│高数│    │英语│    │高数│    │   │
│  [老师▼]       │  ├─────┼────┼────┼────┼────┼────┼────┤   │
│  [专业▼]       │  │10:35│编程│    │物理│    │编程│    │   │
│  [新增][更新]  │  │11:30│    │    │    │    │    │    │   │
│                │  ├─────────────────────────────────────┤   │
│  ┌───────────┐ │  │14:00│    │数据│    │算法│    │    │   │
│  │ 高等数学  │ │  │14:55│实验│结构│网络│    │    │    │   │
│  │ 程序设计  │ │  ├─────┼────┼────┼────┼────┼────┼────┤   │
│  │ 数据结构  │ │  │16:05│    │    │    │    │    │    │   │
│  │ 计算机网络│ │  │17:00│    │    │    │    │    │    │   │
│  └───────────┘ │  └─────┴────┴────┴────┴────┴────┴────┘   │
│   (拖拽到右侧) │                                            │
│                │                                            │
│  [智能排课]    │                                            │
│  [冲突检测]    │                                            │
└────────────────┴────────────────────────────────────────────┘
```

### UI组件设计

#### 1. 课程卡片 (Draggable Card)
```html
<div class="drag-card" draggable="true" data-course="123">
    <div>高等数学</div>
</div>
```
- 可拖拽
- 悬停效果
- 圆角设计

#### 2. 课程表格子 (Timetable Cell)
```html
<div class="tt-cell" data-weekday="1" data-index="2">
    <div class="tt-block">
        <div class="tt-course-name">高等数学</div>
        <div class="tt-info">教师: 张三</div>
        <div class="tt-info">教室: A101</div>
        <span class="tt-del">×</span>
    </div>
</div>
```
- 支持拖放
- 悬停高亮
- 冲突标记

#### 3. 视图模式切换
```
[按班级▼] [2024级计算机1班▼]
[按教师▼] [张三▼]
[按教室▼] [A101▼]
```

---

## 业务逻辑

### 1. 课程创建流程
```
用户输入 → 表单验证 → API调用 → 数据库存储 → 刷新列表
```

**验证规则**:
- 必填: 课程名称、课程类型、授课教师、所属专业
- 自动生成: subject_id (SUB + UUID)
- 唯一性: (name + edu_level)

**跨学院/专业授课支持**:
系统**完全支持**跨学院、跨专业授课：
- ✅ 教师可以给任何学院/专业的学生上课
- ✅ 课程的 `department` 字段表示课程归属（用于统计和管理）
- ✅ 实际授课不受学院/专业限制
- ✅ 前端提供"显示所有教师"选项，方便选择跨学院教师

**典型场景**:
- 数学学院的老师给计算机学院学生上《高等数学》
- 外语学院的老师给全校各学院学生上《大学英语》
- 体育学院的老师给全校学生上《体育》公共课

**代码实现**:
```javascript
async function addCourse() {
    const payload = {
        name: '课程名称',
        course_type: 'required',
        teacher: teacherId,
        department: deptId,
        edu_level: 'undergraduate'
    };
    
    const result = await api('/api/courses/courses/', 'POST', payload);
    loadCourses();  // 刷新列表
}
```

### 2. 排课流程

#### 方式一: 拖拽排课
```
选择课程 → 拖拽到课程表 → 自动校验 → 创建排课记录
```

**步骤**:
1. 用户从左侧课程库拖拽课程
2. 拖到课程表的某个格子
3. 自动获取: 时间段(weekday+index)、周次、班级
4. 调用API创建CourseSchedule
5. 后端校验冲突
6. 成功后刷新课程表

**代码实现**:
```javascript
async function handleDrop(ev) {
    const data = JSON.parse(ev.dataTransfer.getData('text/plain'));
    const weekday = ev.currentTarget.dataset.weekday;
    const index = ev.currentTarget.dataset.index;
    
    const slot = tt.slotByKey[`${weekday}-${index}`];
    
    const payload = {
        school_class: currentTargetId,
        course: data.course,
        teacher: courseObj.teacher,
        timeslot: slot.id,
        week_number: currentWeek
    };
    
    await api('/api/courses/schedules/', 'POST', payload);
    loadSchedule();
}
```

#### 方式二: 智能排课
```
选择课程列表 → 选择班级 → 智能算法 → 批量创建
```

**算法逻辑**:
1. 遍历所有时间段
2. 检查冲突(班级、教师、教室)
3. 评分机制:
   - 必修课优先上午 (+5分)
   - 早节次优先 (+10-index分)
4. 选择最高分时间段
5. 批量创建排课记录

**代码实现**:
```javascript
async function autoSchedule() {
    const payload = {
        school_class: currentTargetId,
        courses: [...tt.selectedCourses],
        start_week: currentWeek,
        end_week: currentWeek,
        week_mode: 'all'
    };
    
    const result = await api('/api/courses/auto-schedule/', 'POST', payload);
    // result.created_count 成功排课数量
}
```

### 3. 冲突检测流程
```
加载当前课程表 → 分析时间段 → 标记冲突 → 高亮显示
```

**检测维度**:
- 教师冲突: 同一教师同一时间有多节课
- 班级冲突: 同一班级同一时间有多节课
- 教室冲突: 同一教室同一时间被占用多次
- 容量冲突: 教室容量 < 班级人数

**代码实现**:
```javascript
async function checkConflicts() {
    const result = await api('/api/courses/schedules/conflicts/?week_number=1&school_class=123');
    
    // result.items = [
    //   {
    //     timeslot: 1,
    //     week_number: 1,
    //     teacher_conflicts: [2, 5],  // 教师ID
    //     class_conflicts: [10],      // 班级ID
    //     room_conflicts: []
    //   }
    // ]
    
    highlightConflicts(result.items);
}
```

---

## API接口

### 课程管理 API

#### 1. 获取课程列表
```
GET /api/courses/courses?q=高数&department=1&college=2
```
**查询参数**:
- `q`: 搜索关键词 (课程名/教师名)
- `department`: 专业ID
- `college`: 学院ID

**响应**:
```json
[
    {
        "id": 1,
        "subject_id": "SUB001",
        "name": "高等数学",
        "course_type": "required",
        "edu_level": "undergraduate",
        "teacher": 5,
        "teacher_name": "张三",
        "department": 10,
        "student_count": 45
    }
]
```

#### 2. 创建课程
```
POST /api/courses/courses/
Content-Type: application/json

{
    "name": "数据结构",
    "course_type": "required",
    "teacher": 5,
    "department": 10,
    "edu_level": "undergraduate"
}
```

#### 3. 更新课程
```
PATCH /api/courses/courses/1/
Content-Type: application/json

{
    "teacher": 6
}
```

#### 4. 删除课程
```
DELETE /api/courses/courses/1/
```

### 排课管理 API

#### 1. 获取课程表
```
GET /api/courses/schedules?school_class=1&week_number=1
GET /api/courses/schedules?teacher=5&week_number=1
GET /api/courses/schedules?classroom=10&week_number=1
```

**响应**:
```json
[
    {
        "id": 100,
        "school_class": 1,
        "course": 5,
        "teacher": 10,
        "classroom": 15,
        "timeslot": 3,
        "week_number": 1
    }
]
```

#### 2. 创建排课
```
POST /api/courses/schedules/
Content-Type: application/json

{
    "school_class": 1,
    "course": 5,
    "teacher": 10,
    "classroom": 15,
    "timeslot": 3,
    "week_number": 1
}
```

**校验**:
- 教师冲突检测
- 班级冲突检测
- 教室冲突检测
- 教室容量检测

#### 3. 移动排课
```
PATCH /api/courses/schedules/100/
Content-Type: application/json

{
    "timeslot": 5
}
```

#### 4. 删除排课
```
DELETE /api/courses/schedules/100/
```

### 智能排课 API

#### 1. 自动排课
```
POST /api/courses/auto-schedule/
Content-Type: application/json

{
    "school_class": 1,
    "courses": [1, 2, 3, 4],
    "start_week": 1,
    "end_week": 18,
    "week_mode": "all",
    "teacher": null,
    "classroom": null
}
```

**参数说明**:
- `week_mode`: all(全部周次) / odd(单周) / even(双周)
- `teacher`: 指定教师(可选,默认用课程的教师)
- `classroom`: 指定教室(可选)

**响应**:
```json
{
    "created_count": 72,
    "items": [
        {
            "course": 1,
            "week": 1,
            "schedule_id": 100,
            "reason": null
        },
        {
            "course": 2,
            "week": 1,
            "schedule_id": null,
            "reason": "班级冲突"
        }
    ]
}
```

#### 2. 冲突检测
```
GET /api/courses/schedules/conflicts/?school_class=1&week_number=1
```

**响应**:
```json
{
    "count": 2,
    "items": [
        {
            "timeslot": 3,
            "week_number": 1,
            "count": 2,
            "schedule_ids": [100, 101],
            "teacher_conflicts": [5],
            "class_conflicts": [],
            "room_conflicts": [10]
        }
    ]
}
```

### 时间段管理 API

#### 1. 获取时间段列表
```
GET /api/courses/timeslots
```

#### 2. 生成标准时间段
```
POST /api/courses/timeslots/generate
```
自动生成标准课表时间段(周一到周五,每天8节课)

---

## 功能流程

### 完整排课流程示例

#### 场景: 为"2024级计算机1班"安排第1周的课程

**步骤1: 创建课程**
```javascript
// 1. 创建"高等数学"课程
await api('/api/courses/courses/', 'POST', {
    name: '高等数学',
    course_type: 'required',
    teacher: 5,           // 张三老师
    department: 10,       // 计算机专业
    edu_level: 'undergraduate'
});
```

**步骤2: 选择视图模式**
```javascript
// 2. 切换到"按班级"视图
document.getElementById('viewMode').value = 'class';
document.getElementById('viewTarget').value = 1;  // 2024级计算机1班
changeViewMode();
```

**步骤3: 手动排课**
```javascript
// 3. 拖拽"高等数学"到周一第2节
// 自动调用 handleDrop()
await api('/api/courses/schedules/', 'POST', {
    school_class: 1,
    course: 1,
    teacher: 5,
    classroom: 15,  // A101教室
    timeslot: 2,    // 周一第2节
    week_number: 1
});
```

**步骤4: 智能排课剩余课程**
```javascript
// 4. 选择多门课程,点击"智能排课"
tt.selectedCourses = new Set([2, 3, 4, 5]);  // 编程、物理、英语、实验
await api('/api/courses/auto-schedule/', 'POST', {
    school_class: 1,
    courses: [2, 3, 4, 5],
    start_week: 1,
    end_week: 1,
    week_mode: 'all'
});
```

**步骤5: 冲突检测**
```javascript
// 5. 检测是否有冲突
const result = await api('/api/courses/schedules/conflicts/?school_class=1&week_number=1');
if (result.count > 0) {
    alert(`发现 ${result.count} 处冲突`);
}
```

---

## 数据流转图

```
用户操作 → 前端JS → API接口 → Django View → 数据库
                                    ↓
                                  权限检查
                                    ↓
                                  数据校验
                                    ↓
                              业务逻辑处理
                                    ↓
                               返回响应
```

### 示例: 创建排课的数据流

```
1. 用户拖拽课程到课程表
   ↓
2. handleDrop() 捕获事件
   ↓
3. 构造payload {school_class, course, teacher, timeslot, week_number}
   ↓
4. POST /api/courses/schedules/
   ↓
5. CourseScheduleViewSet.create()
   ↓
6. CourseScheduleSerializer.validate()
   - 检查教师冲突
   - 检查班级冲突
   - 检查教室冲突
   - 检查教室容量
   ↓
7. 保存到数据库 CourseSchedule.objects.create()
   ↓
8. 返回创建的记录 {id, ...}
   ↓
9. loadSchedule() 刷新课程表
   ↓
10. renderSchedule() 显示新课程
```

---

## 权限控制

### 角色权限矩阵

| 功能 | 超级管理员 | 校长/副校长 | 教务长/副教务长 | 教师 | 学生 |
|------|-----------|------------|----------------|------|------|
| 查看课程 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 创建课程 | ✅ | ✅ | ✅ | ❌ | ❌ |
| 编辑课程 | ✅ | ✅ | ✅ | ❌ | ❌ |
| 删除课程 | ✅ | ✅ | ✅ | ❌ | ❌ |
| 查看课程表 | ✅ | ✅ | ✅ | ✅(自己的) | ✅(自己的) |
| 创建排课 | ✅ | ✅ | ✅ | ✅(自己的) | ❌ |
| 修改排课 | ✅ | ✅ | ✅ | ✅(自己的) | ❌ |
| 删除排课 | ✅ | ✅ | ✅ | ✅(自己的) | ❌ |
| 智能排课 | ✅ | ✅ | ✅ | ❌ | ❌ |
| 冲突检测 | ✅ | ✅ | ✅ | ✅ | ❌ |

### 权限实现

**后端 (Django)**:
```python
class CourseViewSet(viewsets.ModelViewSet):
    permission_classes = [IsAuthenticated, IsAdminOrDeanOrReadOnly]
    
class CourseScheduleViewSet(viewsets.ModelViewSet):
    permission_classes = [IsAuthenticated, IsTeacherOrAdminOrReadOnly]
    
    def get_queryset(self):
        user = self.request.user
        if user.is_superuser:
            return CourseSchedule.objects.all()
        elif user.profile.role == 'teacher':
            return CourseSchedule.objects.filter(teacher=user.profile.teacher_profile)
        elif user.profile.role == 'student':
            return CourseSchedule.objects.filter(school_class=user.profile.student_profile.school_class)
```

---

## 性能优化

### 1. 数据库优化
```python
# 使用 select_related 减少查询次数
Course.objects.select_related('teacher__user_profile__user', 'department__college').all()

CourseSchedule.objects.select_related(
    'school_class', 
    'course', 
    'teacher', 
    'classroom', 
    'timeslot'
).all()
```

### 2. 前端缓存
```javascript
const cache = {
    teachers: null,      // 缓存教师列表
    colleges: null,      // 缓存学院列表
    departments: null,   // 缓存专业列表
    classes: null,       // 缓存班级列表
    rooms: null          // 缓存教室列表
};

// 首次加载后缓存,避免重复请求
async function loadTeachers() {
    if (!cache.teachers) {
        cache.teachers = await api('/api/accounts/teachers');
    }
    return cache.teachers;
}
```

### 3. 防抖搜索
```javascript
// 输入框延迟300ms后才触发搜索
document.getElementById('courseSearch')
    .addEventListener('input', debounce(loadCourses, 300));
```

---

## 错误处理

### 常见错误及处理

#### 1. 冲突错误
```
错误: "教师在该时间段已有课程安排"
处理: 提示用户,并高亮冲突的课程
```

#### 2. 容量错误
```
错误: "教室容量不足(30),班级人数为45"
处理: 提示用户选择更大的教室
```

#### 3. 唯一性错误
```
错误: "该名称的课程已存在"
处理: 提示用户修改课程名称或选择已有课程
```

#### 4. 权限错误
```
错误: "您没有权限执行此操作"
处理: 跳转到登录页或显示权限不足提示
```

### 前端错误处理模式
```javascript
async function addCourse() {
    try {
        await api('/api/courses/courses/', 'POST', payload);
        showMessage('courseMsg', '新增成功', 'success');
        loadCourses();
    } catch (e) {
        showMessage('courseMsg', e.message || '新增失败', 'error');
    }
}
```

---

## 跨学院授课管理

### 业务场景
在实际教学中，经常存在以下跨学院/专业授课场景：

#### 1. 公共基础课
```
数学学院教师 → 全校各专业学生
- 高等数学
- 线性代数
- 概率论与数理统计

外语学院教师 → 全校各专业学生
- 大学英语
- 英语听力
- 英语口语

体育学院教师 → 全校各专业学生
- 体育课程
```

#### 2. 专业交叉课程
```
计算机学院教师 → 电子工程学院学生
- 计算机基础
- 程序设计

经济学院教师 → 管理学院学生
- 经济学原理
- 财务管理
```

### 系统设计支持

#### 数据模型层面
```python
# 教师所属学院/专业（管理维度）
TeacherProfile.department = 数学学院

# 课程归属学院/专业（统计维度）
Course.department = 计算机学院

# 实际授课教师（可跨学院）
Course.teacher = 数学学院的张三老师

# 上课班级（可跨学院）
CourseSchedule.school_class = 计算机学院2024级1班
```

**结论**: 数据库设计本身就支持跨学院授课，无需修改表结构。

#### 前端界面支持
```html
<!-- 新增课程时的教师选择 -->
<select id="courseTeacherCollege">学院筛选</select>
<select id="courseTeacherDept">专业筛选</select>
<select id="courseTeacher">教师选择</select>

<!-- 🆕 显示所有教师选项 -->
<input type="checkbox" id="showAllTeachers" />
<label>显示所有教师（跨学院/专业）</label>
```

**功能说明**:
1. **默认模式**: 根据选择的学院/专业筛选教师（方便快速选择）
2. **跨学院模式**: 勾选"显示所有教师"后，显示全校所有教师

#### 使用流程

**场景: 为计算机1班创建"高等数学"课程，由数学学院张老师授课**

```javascript
// 方式一: 使用筛选（如果知道教师所属学院）
document.getElementById('courseTeacherCollege').value = 2;  // 数学学院
filterTeachersByDept();  // 显示数学学院教师
document.getElementById('courseTeacher').value = 5;  // 张三老师

// 方式二: 显示所有教师
document.getElementById('showAllTeachers').checked = true;
toggleTeacherFilter();  // 显示全校所有教师
document.getElementById('courseTeacher').value = 5;  // 直接选择张三老师

// 创建课程
await api('/api/courses/courses/', 'POST', {
    name: '高等数学',
    course_type: 'required',
    teacher: 5,              // 数学学院的张三老师
    department: 10,          // 课程归属: 计算机专业
    edu_level: 'undergraduate'
});

// 排课（计算机1班）
await api('/api/courses/schedules/', 'POST', {
    school_class: 1,         // 计算机学院2024级1班
    course: 课程ID,
    teacher: 5,              // 数学学院的张三老师
    timeslot: 2,
    week_number: 1
});
```

### 统计与报表

#### 1. 按教师统计（教学工作量）
```sql
-- 查看张三老师的所有授课（可能跨多个学院）
SELECT 
    c.name AS 课程名,
    sc.name AS 班级名,
    d.name AS 班级所属专业,
    col.name AS 班级所属学院
FROM course_schedule cs
JOIN course c ON cs.course_id = c.id
JOIN school_class sc ON cs.school_class_id = sc.id
JOIN department d ON sc.department_id = d.id
JOIN college col ON d.college_id = col.id
WHERE cs.teacher_id = 5
```

#### 2. 按学院统计（接受服务）
```sql
-- 查看计算机学院学生接受了哪些外院教师的课程
SELECT 
    t.teacher_id AS 教师工号,
    u.first_name AS 教师姓名,
    td.name AS 教师所属专业,
    tc.name AS 教师所属学院,
    c.name AS 课程名,
    COUNT(*) AS 课时数
FROM course_schedule cs
JOIN course c ON cs.course_id = c.id
JOIN teacher_profile t ON cs.teacher_id = t.id
JOIN department td ON t.department_id = td.id
JOIN college tc ON td.college_id = tc.id
JOIN school_class sc ON cs.school_class_id = sc.id
JOIN department sd ON sc.department_id = sd.id
JOIN college sc_col ON sd.college_id = sc_col.id
WHERE sc_col.name = '计算机学院'
  AND tc.name != '计算机学院'  -- 外院教师
GROUP BY t.id, c.id
```

### 权限控制

#### 跨学院授课的权限设计
```python
# 教师查看权限
if user.profile.role == 'teacher':
    # 可以查看自己的所有授课（包括跨学院的）
    schedules = CourseSchedule.objects.filter(teacher=user.profile.teacher_profile)

# 学院管理员查看权限
if user.profile.role == 'dean':
    # 可以查看本学院学生的所有课程（包括外院教师授课的）
    schedules = CourseSchedule.objects.filter(
        school_class__department__college=user.profile.college
    )
```

### 最佳实践

#### 1. 公共课程管理
建议为公共课建立专门的"虚拟学院"或统一归类：
```python
# 创建"公共课程"专业
public_dept = Department.objects.create(
    name='公共课程',
    college=通识教育学院
)

# 所有公共课归属此专业
Course.objects.create(
    name='大学英语',
    department=public_dept,
    teacher=外语学院教师
)
```

#### 2. 课程命名规范
```
跨学院课程建议包含受众信息：
- "高等数学(计算机)"
- "高等数学(电子工程)"
- "大学英语(理工类)"
- "大学英语(文科类)"
```

#### 3. 教学班管理
对于大规模公共课，可以：
```python
# 创建教学班（跨行政班）
teaching_class = Class.objects.create(
    name='2024级高等数学1班(理工类)',
    class_type='teaching',  # 教学班
    department=public_dept
)

# 将多个行政班的学生加入教学班
# 然后为教学班排课
```

## 未来扩展

### 计划功能
1. **批量导入**: 支持Excel批量导入课程和排课
2. **课程模板**: 保存常用课程组合为模板
3. **学期管理**: 支持多学期切换
4. **调课申请**: 教师发起调课申请,管理员审批
5. **课程评价**: 学生对课程进行评分和评价
6. **移动端适配**: 响应式设计,支持手机查看课程表
7. **消息通知**: 课程变动时自动通知相关人员
8. **打印导出**: 导出课程表为PDF或图片
9. **🆕 教学班管理**: 支持跨行政班组建教学班
10. **🆕 工作量统计**: 自动统计教师跨学院授课工作量

---

## 总结

### 核心亮点
✅ **直观的拖拽排课**: 所见即所得,操作简单  
✅ **智能算法**: 自动避免冲突,优化排课效率  
✅ **多维度查看**: 灵活切换班级/教师/教室视图  
✅ **实时冲突检测**: 立即发现并标记排课冲突  
✅ **权限分级**: 精细的角色权限控制  
✅ **现代化UI**: 扁平化设计,用户体验好  

### 技术优势
✅ **前后端分离**: RESTful API,易于维护和扩展  
✅ **ORM抽象**: 数据库无关,易于迁移  
✅ **缓存机制**: 减少重复请求,提升性能  
✅ **错误处理**: 完善的异常捕获和用户提示  

---

**文档版本**: 1.0  
**最后更新**: 2025-12-09  
**维护人员**: EduCloud Team

